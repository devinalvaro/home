#!/bin/bash

set -eu

# emacs
rm -rf "$HOME/.emacs.d/.local/cache/"
rm -rf "$HOME/.emacs.d/.local/etc/"

# haskell
rm -f "$HOME/.ghc/ghci_history"

# less
rm -f "$HOME/.lesshst"

# python
rm -f "$HOME/.python_history"
